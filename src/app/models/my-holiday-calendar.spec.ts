import { MyHolidayCalendar } from './my-holiday-calendar';

describe('MyHolidayCalendar', () => {
  it('should create an instance', () => {
    expect(new MyHolidayCalendar()).toBeTruthy();
  });
});
